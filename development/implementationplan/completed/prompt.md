Please create an industry leading themesEngine/global styles in src>features>ui>themes from which we can derive our entire current GUI as a theme, size it with a global style, and color it with a 5 color industry standard color palette as part of the theme.

Use our current theme for all the information. I don’t want to change anything unless it means bringing parity without visual sacrifice.

Once you're done, go through and remove hard codings in lieu of the theme/global styles sizings.

Implementation requirements:
- Preserve existing visual quality and parity.
- Avoid rewrites unless explicitly necessary.
- Make only careful, targeted changes.
- Ensure every phase includes linting and QA/verification checks.
- Use industry-leading structure for theme and global styles.
- Design the solution so the current GUI can derive its sizing and color tokens from a centralized theme system.
