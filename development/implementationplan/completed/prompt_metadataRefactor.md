create a tool script that will migrate all utilities required from within each room metadata tree to a subtree called 'utilities' in addition to migrating each room metadata services required to a subtree called 'services'
verify the script will not create/remove anything except to accomplish the intent here, to better organize each room.
then run the script.

to clarify we are not adding or removing applicable utilities or services for any rooms just organizing them better.
the purpose of this is so that you dont edit the file yourself and information is preserved.
